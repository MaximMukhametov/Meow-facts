pipeline {
    agent {
        docker { image 'python:3.7-slim' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'python -V'
                sh 'ls -la'
                sh 'pwd'
                sh 'cd server/'
                sh 'ls -la'
                sh 'pwd'
                sh 'python3 -m venv venv'
                sh 'source venv/bin/activate'
                sh 'python -m pip install -r server/requirements.txt --user'
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                sh 'pytest'
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}