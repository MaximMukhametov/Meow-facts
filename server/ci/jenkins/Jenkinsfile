pipeline {
    agent {
    dockerfile {
        filename 'server/Dockerfile'
        args '/bin/bash'
    }
}
    stages {
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'cd server/ && pytest'
            }
        }
    }
}