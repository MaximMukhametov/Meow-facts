pipeline {
    agent {
    dockerfile {
        filename 'server/Dockerfile'
        args '/bin/bash'
    }
}
    stages {
        stage('Test') {
            steps {
                sh 'pwd'
                sh 'ls -la'
                echo 'Testing..'
                sh 'pytest server/'
            }
        }
    }
}